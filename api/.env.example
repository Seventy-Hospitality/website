# Database
DATABASE_URL="postgresql://user:password@localhost:5432/seventy?schema=public"

# Auth
JWT_SECRET="generate-a-64-char-random-string-here"

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Vercel Cron
CRON_SECRET="generate-a-random-string-here"

# Resend
RESEND_API_KEY=re_...

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Web App
WEB_URL=http://localhost:5173

# Media
MEDIA_BACKEND=local
MEDIA_S3_BUCKET=
MEDIA_S3_REGION=us-east-1
MEDIA_S3_PREFIX=
MEDIA_STALE_UPLOAD_MAX_AGE_HOURS=24
MEDIA_STALE_UPLOAD_CLEANUP_LIMIT=10

# Native OAuth (Club70 app sign-in). The API boots without these; the
# /api/auth/oauth endpoints return NOT_CONFIGURED until they are set.
# Comma-separated allowlist of Google client IDs (iOS, Android, web).
GOOGLE_OAUTH_CLIENT_IDS=
# Apple Sign in: bundle ID is required to verify identity tokens; team ID,
# key ID and the .p8 private key (newlines escaped as \n) enable the
# authorization-code exchange that stores the refresh token for /auth/revoke.
APPLE_BUNDLE_ID=
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY=
