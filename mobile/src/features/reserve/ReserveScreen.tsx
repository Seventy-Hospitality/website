import { PlaceholderScreen } from '../PlaceholderScreen';

export function ReserveScreen() {
  return (
    <PlaceholderScreen
      title="Reserve"
      subtitle="Book a court or amenity"
      owner="M3"
      body="The booking wizard (resource-type list, availability rail, multi-slot select, invite players, quote, PaymentSheet checkout, and confirmation) is built by the M3 package against the new reservation engine wired up here."
    />
  );
}
