import { PlaceholderScreen } from '../PlaceholderScreen';

export function HomeScreen() {
  return (
    <PlaceholderScreen
      title="Home"
      subtitle="Your club at a glance"
      owner="M2"
      body="The home feed (greeting, quick-book, upcoming reservations with inline invitation actions, club invitations, spotlight events, and the member QR card) lands with the M2 package on top of this foundation."
    />
  );
}
