import { PlaceholderScreen } from '../PlaceholderScreen';

export function ClubsScreen() {
  return (
    <PlaceholderScreen
      title="Clubs"
      subtitle="Your social clubs"
      owner="M5"
      body="Club list, the create wizard, detail + activity, the members roster, invitations (member picker, copy link, QR, share), and join-via-link are delivered by the M5 package. The clubs API methods and invite-QR primitive are ready here."
    />
  );
}
